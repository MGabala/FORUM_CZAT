@page "/admin"
@model FORUM_CZAT.Pages.AdminApprovalPageModel
@{
}
<h1>Test</h1>
<table class="table">
    <tbody>
        @foreach(var item in Model.Posts)
        {
            @item.Title
        }
    </tbody>
</table>